import { defineConfig } from 'astro/config';

// Once your domain is registered, set it here — it helps with SEO and social sharing previews.
export default defineConfig({
  site: 'https://waihekemakers.co.nz',
});
